
docker images

docker build -t <username>/<app> .

docker inspect <container>
docker logs <container>

docker exec -it <container> bash
docker exec -it <container> /bin/bash
docker exec -it <container> cat /etc/os-release
winpty docker exec -it <container> bash

docker login --username=yourhubusername --email=youremail@company.com

docker ps
docker ps -a
docker ps -aq
docker rm -f $(docker ps -aq)

docker tag bb38976d03cf <username>/<app>:firsttry

docker run -p 49160:3000 -d --name boomstick garmin/boomstick
docker run -p 49160:3000 -d --name boomcrutch some-registry.io/garmin/boomcrutch:1.2.3
docker run -p 49160:6379 --name some-redis -d redis redis-server --appendonly yes

docker run --rm -d --name <preferred_name> <image>:<version>

docker create
docker start <container>
docker stop <container>

docker-compose up
docker-compose up -d									// detached
docker-compose up -d --build							// clean builds containers
docker-compose -f <alt-docker-compose-file> up
docker-compose down
docker-compose create <container>
docker-compose start <container>
docker-compose nework create <network>
docker-compose nework create docker_default
